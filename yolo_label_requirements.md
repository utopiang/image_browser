## 1. 文档信息
| 项目   | 内容             |
| ---- | -------------- |
| 文档名称 | YOLO标签筛选功能需求文档 |
| 版本   | v1.0           |
| 日期   | 2026-05-14     |
| 关联项目 | 图片浏览筛选工具       |
## 2. 项目背景
现有图片浏览筛选工具已支持对预测结果图片进行人工筛选（误检/漏检标记、复制/移动/批量操作）。现需增加 YOLO格式标签可视化与筛选 功能，将标签以图形化方式绘制在图片上，辅助人工快速判断检测质量，并支持对标签及对应图片进行同步筛选管理。
## 3. 术语与定义
| 术语              | 说明                                              |
| --------------- | ----------------------------------------------- |
| **YOLO标签**      | 归一化坐标格式的目标标注文件（`.txt`），每行一个对象                   |
| **classes.txt** | 类别映射文件，每行一个类名，行号（从0开始）即为类别索引                    |
| **同名关联**        | 图片文件与标签文件主文件名相同，仅扩展名不同（如 `001.jpg` ↔ `001.txt`） |
| **标签类型**        | detect（检测框）、obb（旋转框）、segment（分割）、pose（姿态）       |
## 4. 功能需求（总体）
| 需求编号 | 需求描述                                        | 优先级 |
| :--: | ------------------------------------------- | :-: |
|  R1  | 支持选择图片文件夹、标签文件夹、classes.txt 文件              |  P0 |
|  R2  | 支持4种YOLO标签类型的可视化绘制（detect/obb/segment/pose） |  P0 |
|  R3  | 标签文件暂只支持 `.txt` 格式，架构预留其他格式扩展接口             |  P0 |
|  R4  | 沿用现有筛选逻辑：标记（生成txt）、复制、移动、批量操作               |  P0 |
|  R5  | 通过 classes.txt 建立类别索引与类别名的映射关系              |  P0 |
|  R6  | 图片与标签同步操作（复制/移动图片时同步处理同名标签）                 |  P1 |
|  R7  | 标签显示/隐藏、类别过滤、颜色配置、悬停提示                      |  P1 |
|  R8  | 标签格式校验、缺失标签提示、错误容错                          |  P1 |
|  R9  | 标签统计面板（单图统计+数据集统计）                          |  P2 |
## 5. 详细功能需求
### 5.1 文件选择模块
|  编号  | 需求点            | 详细说明                                                                      |
| :--: | -------------- | ------------------------------------------------------------------------- |
| R1.1 | 图片文件夹选择        | 选择包含待筛选图片的文件夹，支持递归/非递归读取，支持格式：`jpg`, `jpeg`, `png`, `bmp`                 |
| R1.2 | 标签文件夹选择        | 选择包含 `.txt` 标签文件的文件夹，**独立于图片文件夹**                                         |
| R1.3 | classes.txt 选择 | 选择类别映射文件，每行一个类名，行号即为类别索引（从0开始）。未选择时，默认显示 `class_0`, `class_1`...          |
| R1.4 | 同名关联规则         | 图片与标签通过**主文件名（不含扩展名）**&#x5339;配，匹配不区分大小写。如 `IMG_001.jpg` 匹配 `img_001.txt` |
### 5.2 标签可视化模块
|   编号  | 需求点               | 详细说明                                                                                                                                         |
| :---: | ----------------- | -------------------------------------------------------------------------------------------------------------------------------------------- |
|  R2.1 | **detect（目标检测）**  | 绘制水平矩形框。YOLO格式：`class_id x_center y_center width height [confidence]`                                                                        |
|  R2.2 | **obb（旋转目标检测）**   | 绘制旋转四边形。YOLO OBB格式：`class_id x1 y1 x2 y2 x3 y3 x4 y4 [confidence]`（归一化坐标，按顺序连接）                                                              |
|  R2.3 | **segment（实例分割）** | 绘制多边形轮廓。YOLO格式：`class_id x1 y1 x2 y2 ... xn yn [confidence]`                                                                                 |
|  R2.4 | **pose（姿态估计）**    | 绘制检测框 + 关键点 + 骨架连线。YOLO格式：`class_id x_center y_center width height x1 y1 v1 x2 y2 v2 ... [confidence]`。`v` 为可见性：0=不可见（不绘制），1=可见，2=遮挡（可选不同颜色） |
|  R2.5 | 标签绘制联动            | 图片缩放、拖动时，标签绘制层同步变换，保持坐标对应关系                                                                                                                  |
|  R2.6 | 标签显示开关            | 提供全局"显示标签/隐藏标签"切换按钮（快捷键建议 `T`）                                                                                                               |
|  R2.7 | 类别过滤              | 提供类别复选框列表，勾选则显示该类标签，取消则隐藏                                                                                                                    |
|  R2.8 | 颜色配置              | 每个类别自动分配不同颜色；提供配置入口允许用户自定义颜色                                                                                                                 |
|  R2.9 | 绘制样式              | 可配置：框线粗细（1~5px）、填充透明度（0~50%）、关键点半径、字体大小                                                                                                      |
| R2.10 | 悬停信息              | 鼠标悬停在标签上时，显示Tooltip：类别名、类别索引、坐标信息、置信度（若存在）                                                                                                   |

### 5.3 标签筛选与操作模块
|  编号  | 需求点       | 详细说明                                                       |
| :--: | --------- | ---------------------------------------------------------- |
| R4.1 | 沿用现有筛选逻辑  | 保持原有"误检"、"漏检"标记功能，标记结果保存为 `误检.txt`、`漏检.txt`，每行记录图片**绝对路径** |
| R4.2 | 图片-标签同步复制 | 点击"复制"将当前图片复制到目标目录时，**自动查找并复制同名的 `.txt` 标签文件**到目标目录的同名子路径  |
| R4.3 | 图片-标签同步移动 | 点击"移动"将当前图片移动到目标目录时，**自动查找并移动同名的 `.txt` 标签文件**             |
| R4.4 | 批量操作扩展    | 批量复制/移动时（基于 `误检.txt` 等路径列表），对列表中每个图片路径同步处理其同名标签文件          |
| R4.5 | 孤立标签处理    | 若标签文件夹中存在无法匹配到图片的标签文件，在状态栏或日志区提示孤立标签数量                     |
### 5.4 类别映射模块
|  编号  | 需求点            | 详细说明                                                                |
| :--: | -------------- | ------------------------------------------------------------------- |
| R5.1 | classes.txt 解析 | 按行读取，行号=类别索引，内容=类别名。去除首尾空白，忽略空行                                     |
| R5.2 | 索引越界处理         | 若标签中的 `class_id` 超出 classes.txt 行数范围，显示为 `unknown(class_id)`，并用灰色标识 |
| R5.3 | 无 classes.txt  | 未选择时，类别名默认显示为 `class_0`, `class_1`, ...                             |
### 5.5 批量操作模块（与现有功能兼容）
|  编号  | 需求点          | 详细说明                                                     |
| :--: | ------------ | -------------------------------------------------------- |
| R4.6 | 批量操作入口       | 在现有批量操作界面增加"同步处理标签文件"复选框，默认勾选                            |
| R4.7 | 标签独立批量操作（预留） | 支持选择 `error_labels.txt`（记录标签文件路径），对标签文件执行批量复制/移动（V2.0可选） |
## 数据格式规范
### 标签文件格式（.txt）
所有坐标均为归一化值（0~1），基于图片宽高转换为像素坐标绘制。
| 类型      | 每行格式                                              | 绘制方式                                          |
| ------- | ------------------------------------------------- | --------------------------------------------- |
| detect  | `class_id xc yc w h [conf]`                       | 水平矩形框 `(xc-w/2, yc-h/2)` 到 `(xc+w/2, yc+h/2)` |
| obb     | `class_id x1 y1 x2 y2 x3 y3 x4 y4 [conf]`         | 按顺序连接四个点形成四边形                                 |
| segment | `class_id x1 y1 x2 y2 ... xn yn [conf]`           | 连接所有点形成闭合多边形                                  |
| pose    | `class_id xc yc w h kp1_x kp1_y kp1_v ... [conf]` | 矩形框 + 关键点（按预定义骨架连线）                           |
[conf] 为可选置信度字段，存在时显示在标签Tooltip中。
### 6.2 classes.txt 格式
```
class_0
class_1
class_2
...
class_n
```
第0行 → 索引0
第1行 → 索引1
以此类推
## 7. 非功能需求
|  编号 | 需求    | 说明                                          |
| :-: | ----- | ------------------------------------------- |
|  N1 | 性能    | 单图标签数 ≤ 500 时，缩放/拖动帧率 ≥ 30fps               |
|  N2 | 容错    | 标签文件某行格式错误时，跳过该行并提示，不中断程序                   |
|  N3 | 扩展性   | 标签解析器采用策略模式/插件架构，便于后续支持 `.json`, `.xml` 等格式 |
|  N4 | 状态持久化 | 记录用户选择的图片文件夹、标签文件夹、classes.txt 路径，下次启动自动恢复  |
|  N5 | 兼容性   | 与现有筛选功能完全兼容，不破坏原有 `误检.txt` / `漏检.txt` 格式    |
## 8. 界面需求（新增/调整）
| 区域       | 需求                                               |
| -------- | ------------------------------------------------ |
| 顶部工具栏    | 增加【选择标签文件夹】、【选择classes.txt】、【显示/隐藏标签】、【类别过滤】下拉按钮 |
| 左侧边栏     | 增加"类别列表"面板：显示所有类别及对应颜色方块，支持勾选显示/隐藏               |
| 右侧边栏（可选） | 增加"标签统计"面板：当前图片各类别数量、总标签数                        |
| 主视图      | 在图片上叠加绘制标签（框、多边形、关键点），层级在图片之上                    |
| 底部状态栏    | 显示：当前图片名、图片尺寸、匹配的标签文件数、孤立标签提示                    |
| 设置对话框    | 标签样式配置：线条粗细、透明度、颜色方案、关键点大小                       |
### 9. 附录：快捷键建议（新增）
| 快捷键   | 功能                   |
| ----- | -------------------- |
| `T`   | 显示/隐藏标签绘制层           |
| `F`   | 打开/关闭类别过滤面板          |
| 原有快捷键 | 保留原有上下张、标记、复制、移动等快捷键 |
